const idleSeconds = int.fromEnvironment('IDLE_SECONDS', defaultValue: 180);
const idleWarnSeconds = int.fromEnvironment('IDLE_WARN_SECONDS', defaultValue: 30);
const sessionMaxSeconds = int.fromEnvironment('SESSION_MAX_SECONDS', defaultValue: 1200);
